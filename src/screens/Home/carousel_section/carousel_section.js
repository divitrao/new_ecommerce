import React from "react";
import { Text, View } from "react-native";
import styles from './styles'

const  Carousel_card=()=>{
 return(
    <View style={styles.card_view}>
        <Text style={{fontSize:50,padding:10}}>70 %</Text>
    </View>
    );
}

export default Carousel_card